import tkinter as tk
import random

MOVE_LEFT = "<MoveLeft>"
MOVE_RIGHT = "<MoveRight>"
MOVE_UP = "<MoveUp>"
MOVE_DOWN = "<MoveDown>"

    if should_move_left():
        window.event_generate(MOVE_LEFT)